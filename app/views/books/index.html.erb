<% if policy(Book).create? %>
  <%= link_to "Add a book", books_new_path %>
<% end %>
<% @books.each do |book| %>
  <ul>
    <li><%= book.title %> </li>
    <li> <%= image_tag book.cover_picture %> </li>
    <li> <%= book.ISBN %> </li>
  </ul>
<% end %>


