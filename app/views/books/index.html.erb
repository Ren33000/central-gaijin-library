

<%= render 'shared/book-frame' %>







<div class="btn btn-light">
<% if policy(Book).create? %>
  <i class="fas fa-book"></i>
  <%= link_to "Add a book", books_new_path, :class => "btn" %> 
  
<% end %>
</div>









